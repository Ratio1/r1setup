## Final UI E2E Check

Timestamp: `2026-03-20T09:42:40+02:00`

Environment:
- repo-local `r1setup`
- dev home: `/tmp/r1setup-dev`
- active config: `reval-devnet_20260320_0147_1n`
- live hosts:
  - `35.228.69.214`
  - `34.88.90.109`

Goal:
- rerun the two final UI fixes on real hosts after commit `d3dbfb1`

## Scenarios

1. Fresh startup
- result: pass
- first render immediately showed:
  - `reval-devnet_20260320_0147_1n | devnet | 📡 tracking 1 live node(s)`
- the stale `✗ not deployed` compact label did not reappear

2. Same-machine undeployed sibling
- added `nodeb` on the same machine as `nodea` (`35.228.69.214`)
- result: pass
- `Node Status & Info` showed:
  - `nodea` as `RUNNING`
  - `nodeb` as `NOT DEPLOYED`
  - `nodeb` service version state as `[N/A]`
- no false `Recommended Actions -> Update service for: nodeb` block was shown

3. Cleanup
- removed `nodeb`
- result: pass
- config returned to the single-node state
- explicit expert-mode retention message was still shown as expected

## Conclusion

The two remaining operator-facing UI issues from the previous live revalidation are resolved on real hosts:
- compact startup deployment text is correct on first render
- undeployed sibling nodes no longer trigger false service-update recommendations
